var gameState = null;
var interval;

$(document).ready(function(){
    $('#waiting').show();
    /* sync(function(){ */
    /*     console.log(gameState); */
    /* }); */
    interval = setInterval(sync, 1000*1);
});


function sync(){
    $.getJSON('@{GameStateR}', function(data){
        gameState = data;
        console.log(gameState.self);
    });
}
